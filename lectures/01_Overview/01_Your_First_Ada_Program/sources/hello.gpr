project Hello is

   for Main use ("hello");
   for Object_Dir use "obj";
   for Exec_Dir use ".";

   package Compiler is
      for Default_Switches ("Ada") use ("-gnat12");
   end Compiler;

end Hello;
